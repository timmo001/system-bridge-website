---
title: Overview
description: Guides and reference for installing, running, and integrating with System Bridge.
---

import { CardGrid, LinkCard } from '@astrojs/starlight/components';

export const gettingStarted = [
  { title: 'Install', href: '/install/', description: 'Download and install System Bridge on Windows or Linux.' },
  { title: 'Running', href: '/running/', description: 'Start System Bridge and keep it running.' },
  { title: 'CLI', href: '/cli/', description: 'Control System Bridge from the command line.' },
];

export const apiCards = [
  { title: 'API Reference', href: '/api/', description: 'Base URL, authentication and the three protocols.' },
  { title: 'Status & Health', href: '/api/status/', description: 'Check whether the backend is running.' },
  { title: 'Data', href: '/api/data/', description: 'Request system data over the HTTP API.' },
  { title: 'Media File Data', href: '/api/media-file-data/', description: 'Access media file information.' },
  { title: 'MCP Server', href: '/api/mcp/', description: 'Connect AI assistants over the Model Context Protocol.' },
];

export const websocketCards = [
  { title: 'Overview', href: '/api/websocket/', description: 'Connection URL, message envelopes and errors.' },
  { title: 'Data', href: '/api/websocket/data/', description: 'Request, subscribe to and receive system data.' },
  { title: 'Input', href: '/api/websocket/input/', description: 'Send keyboard keypresses and text.' },
  { title: 'Control', href: '/api/websocket/control/', description: 'Media, notifications, opening files/URLs and power.' },
  { title: 'System', href: '/api/websocket/system/', description: 'Files, settings, commands and exiting the app.' },
];

System Bridge runs on your desktop and exposes your system over a local API and WebSocket. Use these guides to get set up and start integrating.

## Getting Started

<CardGrid>
  {gettingStarted.map((card) => <LinkCard key={card.href} {...card} />)}
</CardGrid>

## API

<CardGrid>
  {apiCards.map((card) => <LinkCard key={card.href} {...card} />)}
</CardGrid>

## WebSocket

<CardGrid>
  {websocketCards.map((card) => <LinkCard key={card.href} {...card} />)}
</CardGrid>
